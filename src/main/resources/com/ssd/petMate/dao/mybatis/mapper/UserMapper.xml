<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper interface랑 경로를 똑같이 정함 -->
<mapper namespace="com.ssd.petMate.dao.mybatis.mapper.UserMapper">
  <cache />

	<!-- 회원 정보 가져오기  -->
	<select id="getUserByUserIDAndPassword" parameterType="User">
		SELECT *
		FROM UserList
		WHERE userID = #{userID} AND pwd = #{pwd}
		LIMIT 1;
	</select>
	
  	<!-- 회원 가입  -->
	<insert id="insertUser" parameterType="User">
		INSERT
		INTO UserList (userID, pwd, email, userType)
		VALUES (#{userID}, #{pwd}, #{email}, #{userType})
	</insert>

  	<!-- 아이디 중복 확인 -->
	<select id="getUserByUserID" parameterType="User" resultType="Integer">
		SELECT COUNT(*)
		FROM userList
		WHERE userID = #{userID}
	</select>
	
	<!-- 회원 정보 수정  -->
	<update id="updateUser" parameterType="User">
		UPDATE UserList SET  
		email = #{email},  
		pwd = #{pwd},  
		userType = #{userType}  
		WHERE userID = #{userID}
	</update>
	
	<!-- 회원 탈퇴  -->
	<delete id="deleteUser">
		DELETE FROM userList
		WHERE userID = #{userID}
	</delete>
</mapper>